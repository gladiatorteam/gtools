import 'package:flutter/material.dart';

void main(List<String> args) {
  runApp(const GTools());
}

class GTools extends StatelessWidget {
  const GTools({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      title: 'GTools',
    );
  }
}
